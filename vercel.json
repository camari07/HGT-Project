{
  "buildCommand": "cd LogTracker && npm install && npm run build",
  "outputDirectory": "LogTracker/dist",
  "installCommand": "cd LogTracker && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
